{hooks, [
    {procloc, [
        {generic, mod_coords, 20},
        {generic, mod_mapping, 22},
        {generic, device_SUITE, handler_trail, 25},
        {generic, device_SUITE, handler_maybe_error, 30}
    ]},
    {save_state, [{mod_db, handle_newstate, 50} ,
                  {device_SUITE, maybe_crash_while_saving, 30}]
    },
    {publish_event, [{event_listener, publish_event, 50}]
    }
]}.
{idle_timeout, 2}. % in seconds
{sensors, #{sensor_a => 1, sensor_b => 1}}.
{device, [
    {formula, [
        {sensors, #{sensor_b => 2, sensor_c => 3}}
    ]}]
}.